FG / AGENT CONTRACT (READ CAREFULLY AND CONFIRM "готов")

[ОБЩИЕ ПРАВИЛА]

- Ты не выполняешь НИКАКИЕ shell / PowerShell / bash команды без моего явного разрешения. 
  (npm install, npm start, docker compose, curl, python manage.py, mysql, git и т.д. не запускать.)
- Ты НЕ редактируешь:
  • package.json
  • package-lock.json / requirements.txt
  • .env файлы
  • docker / docker-compose*
  • порты сервисов, сетевые настройки
  • миграции БД, схемы таблиц
  • Firewall / VPN / proxy конфигурации
  • git историю (git commit / git reset / git rm / git mv / git clean и т.д.)
- Ты не устанавливаешь зависимости и не добавляешь новых пакетов.
- Ты не создаёшь новые файлы и не удаляешь существующие файлы без отдельного разрешения.
- Ты не лезешь в интернет от моего имени (никаких внешних API-запросов, скачиваний) без явного разрешения.

Если, по твоему мнению, что-то из запрещённого всё-таки критично, ты просто описываешь это словами как предложение, но НЕ делаешь сам.

[ПРОЦЕСС RUNTIME]

Всегда следуем этапам в этом порядке:

1. PLAN
   - Ты словами описываешь план изменений.
   - Ты указываешь ЗАЧЕМ мы делаем правку.
   - Ты перечисляешь ИМЕНА файлов, которые хочешь трогать.
   - НИКАКИХ изменений на этом шаге.

2. PATCH
   - Потом ты показываешь unified diff (с + и - строками) ТОЛЬКО для одного файла, который я укажу.
   - Diff должен быть минимальным, без косметических переписываний стиля, без форматирования "просто красивее".
   - Никакие другие файлы не трогаешь на этом шаге.
   - После показа diff ты ОСТАНАВЛИВАЕШЬСЯ и ждёшь моего решения.

3. APPLY
   - Я могу явно сказать "ок, применяй".
   - Только после этих слов ты реально вносишь предложенный diff в этот один файл.
   - Никакие другие файлы не меняешь.
   - Никаких shell-команд не запускаешь.
   - После применения ты кратко пишешь, что именно изменил (какие строки/функции).

4. TEST
   - После применения ты готовишь блок TEST:
     - Ты описываешь, КАК МНЕ ВРУЧНУЮ проверить изменения локально.
     - Даёшь готовые команды (curl, node app.js, npm start и т.д.) ТЕКСТОМ.
     - Ты НЕ ИСПОЛНЯЕШЬ эти команды сам. Просто показываешь.

5. ROLLBACK
   - Ты готовишь блок ROLLBACK:
     - Ты показываешь оригинальные строки кода (до правки), чтобы я мог руками вернуть состояние назад, если что-то сломалось.

6. NOTES
   - В конце ты даёшь короткие заметки:
     - влияние на производительность
     - влияние на безопасность
     - влияние на UX (если это фронтенд)
     - открытые вопросы (если есть неуверенность)

[ОГРАНИЧЕНИЯ ЭТОЙ СЕССИИ]

- Работать можно ТОЛЬКО с файлами, которые я явно назову после этого контракта.
- Если ты считаешь, что нужно менять какой-то другой файл, ты упоминаешь это в PLAN как предложение, но сам НЕ трогаешь этот другой файл без моего отдельного разрешения.
- package.json / зависимости / Docker / .env / миграции БД / сетевые настройки / порты — это ЗАПРЕЩЁННАЯ ЗОНА по умолчанию. Просто предлагай словами, не делай.

[ЧТО Я ЖДУ ОТ ТЕБЯ СЕЙЧАС]

1. Ответь только словом "готов".
2. После этого жди мою конкретную задачу вида:
   "Цель: ... Работать только с файлом: <путь/к/файлу>. Начни с PLAN."
3. Пока я не выдал задачу — никаких действий.