# Отчёт по доработкам

## Основные изменения
- Инкапсулировали layout графика в `ChartShell` и подключили новый `ChartCanvas`, убрав устаревший `ChartSurface`.
- Подключили единый компонент `Overlay` для поисковой модалки и фонового затемнения.
- Добавили хук `useDropdown` и перевели на него выпадающие меню интервалов и таймфреймов в `ChartToolbar`.
- Вынесли разметку тулбара/сцены в `chart-host` и добавили глобальную вертикальную рельсу с привязкой к токенам.
- Обновили стили expanded-режима: тулбар теперь «капсула», выровненная влево, без подложки, график имеет стеклянную карточку.
- Привели CSS к токенам (`--dock-left-width`, `--gutter-width`, `--host-pad-x`), убрали дубли и артефакты после скриптовых правок.
## Файлы
- `src/components/Results/Chart/ChartRenderer.js`
- `src/components/Results/Chart/ChartShell.js`
- `src/components/Results/Chart/ChartToolbar.js`
- `src/components/Results/Chart/ChartToolbar.css`
- `src/components/Results/Chart/ChartLayout.css`
- `src/components/Overlay/Overlay.js`
- `src/components/SearchModal/SearchModal.js`
## Результат
График в expanded-режиме выглядит как «карточка» с единой вертикальной направляющей; верхняя панель лёгкая, без фона, стоит слева и совпадает с карточкой. Навигационные выпадающие списки управляются единым примитивом, а модальные/фоновые слои используют общий Overlay.
## Изменения 2025-10-29
- Перевели `TickerCard` в режим «паспорт»: карточка подтягивает нормализованный `instrumentMeta`, выводит Identity → Chips → див. подсветку → сессию → действия и адаптируется по брейкпоинтам (≤960/≤640).
- Добавили стили `TickerCard.css`: новые токены цвета/отступов, табличные цифры в чипах, вертикальные брейкпоинты, разделители для групп и кнопки действий.
- Переработали `TopMetricsBar`: табличные цифры, группировка (Primary/Secondary/Tertiary), стрелки у Δ, цвет только на изменении, fade/flash при hover/стриме с учётом `prefers-reduced-motion`.
- Добавили фичу-флаг: состояние хранится в `localStorage` (`fg.topMetrics.enabled`), можно задавать через URL (`?tm=off` / `?tm=on`). В консоли доступно `window.__fgTopMetricsToggle(true|false)`.
- Нормализовали мелкие Δ (`PRICE_EPS=1e-6`, `PCT_EPS=0.005`), чтобы цвет и анимация не срабатывали на шумах.
- Вернули явный бордер правой шкалы (`rgba(126,134,152,0.65)` в expanded, `rgba(48,52,64,0.35)` в компактном) — визуальная рельса снова стабильна.
- Переработали `TopMetricsBar`: табличные цифры, группировка (Primary/Secondary/Tertiary), стрелки у Δ, цвет только на изменении, лёгкие fade/flash при hover/стриме с учётом `prefers-reduced-motion`.
- Добавили пользовательский флаг (`?tm=on|off`, локальное `localStorage`, `window.__fgTopMetricsToggle`) и сохранили master kill switch. При отключении возвращаются тултип и цена в TickerCard.
- Нормализовали мелкие Δ (`PRICE_EPS=1e-6`, `PCT_EPS=0.005`), чтобы цвет и анимация не срабатывали на шум.
- Вернули явный бордер правой шкалы (`rgba(126,134,152,0.65)` / `rgba(48,52,64,0.35)`) — визуальная рельса теперь стабильна.

## Изменения 2025-10-30
- Переписали подсказки в `TickerCard`: используем SVG-«i» из компонента `Price`, показываем полное название сектора/сессии, подсказки не смещают layout.
- Подправили `SearchForm`: список подсказок позиционируется абсолютно и не вызывает Cumulative Layout Shift.
- Оптимизировали `ChartCanvas`: отключаем hover/tooltip во время зума, ограничили частоту обновлений кроссхейра до 30 fps и снизили чувствительность колеса. График масштабируется без `[Violation] requestAnimationFrame` и лагов на таймфрейме 6m × 1m.
## Изменения 2025-10-05
- Добавили DOM-узел `toolbar-scrim` под верхним тулбаром и усилили слой scrim (градиент, blur, токены высоты).
- Перестроили токены в `ChartLayout.css`: ширина желоба, параметры рельсы, высота scrim, порядок z-index (сцена → рельса → scrim → тулбар).
- Обновили позиционирование рельсы (left, z-index 10) и сместили левую док-панель ближе к краю (`margin-left: calc(var(--host-pad-x) - 30px)` + внутренний padding).
- Для `ChartToolbar.css` закрепили секцию на `position: relative`, добавили псевдослои `::before`/`::after` (шторка + антишов), привязали капсулу к `--chart-left + toolbar-margin`, отключили тени/filters в expanded.
- Привели стили капсулы к прозрачному состоянию, гарантировали что все футеры меню/кнопок без box-shadow/drop-shadow в expanded.
## Проверить
- Expanded-режим: шторка перекрывает фон, строки под тулбаром не просвечивают, рельса остаётся по центру желоба.
- Левый док-инструментов не пересекает рельсу (проверить 1x и 2x масштаб).
- Выпадающие меню и капсула не конфликтуют со scrim (z-index 30).
## Изменения 2025-10-06
- Перевели `TickerCard` в оверлейный слой: карточка рендерится через `overlay-left`, не участвует в сетке и больше не двигает график/капсулу.
- Вернули стабильные токены паза (`--gutter-width: 32px`) и добавили `--overlay-gap`, `--chart-gap-from-rail` для точной настройки зазора между рельсой и карточкой.
- Убрали макетный левый слот тулбара и запретили ему влиять на layout (`display: none`).
- Для верхней панели в expanded-режиме закрепили выравнивание справа, задали фиксированный `margin` и `width: fit-content`; капсула теперь «парит» у рельсы без фоновых подложек.
- Сузили `TickerCard` (220px / 200px на <1360px) и синхронизировали минимальную ширину слота, чтобы карточка корректно вписывалась в окно оверлея.
- Обновили `ChartRenderer.js` для передачи актуальных данных в карточку и удаления left-slot из DOM-сетки.
- Expanded-режим: карточка остаётся слева поверх сцены, при кликах работает `onSearch`, график и тулбар не смещаются.
- При ресайзе до 1440/1280px карточка корректно вписывается в окно, капсула тулбара держится рядом с рельсой.
- На узких экранах убедиться, что скрытый left-slot не оставляет пустых отступов и overlay не перекрывает интерактивные элементы графика.
## Изменения 2025-10-13
### Что сделали
- Рефакторинг `ChartToolbar.js`: убран локальный `SearchModal`, добавлен проп `onOpenSearch`, все кнопки поиска вызывают единый обработчик без передачи `event` или «тяжёлых» объектов.
- Обновлён `ChartContent.js`: добавлен мемоизированный `handleOpenSearch`, который прокидывает вызов в родительский `onToggleSearch` и передаёт его в `ChartRenderer`.
- `ChartRenderer.js` теперь принимает `onOpenSearch`, передаёт его в тулбар и `TickerCard`, что обеспечивает одну точку управления модалкой поиска.
- `TickerCard.js`: обработчик клика блокирует всплытие и открывает поиск; добавлена подсказка с `aria-describedby` и позиционированием справа.
- `TickerCard.css`: добавлены стили всплывающей подсказки (`tc-tooltip--east`) с плавной анимацией, стрелкой и отключением на устройствах без hover.
### Файлы и ответственность
- `src/components/Results/Chart/ChartToolbar.js` — рендер панели управления графика; теперь слушает `onOpenSearch` и не монтирует модальные окна.
- `src/components/Results/Chart/ChartContent.js` — обвязка над `ChartRenderer`, мемоизирует обработчики и прокидывает `handleOpenSearch` во вложенные компоненты.
- `src/components/Results/Chart/ChartRenderer.js` — размещает тулбар, карточку тикера и канвас; указывает единую функцию открытия поиска обеим кнопкам.
- `src/components/Results/Chart/TickerCard.js` — карточка инструмента; заботится об очищенном клике и выводит подсказку.
- `src/components/Results/Chart/TickerCard.css` — визуал карточки и тултипа: позиционирование, анимация, стрелка и отключение на тач-экранах.
- Пакет `src/components/Results/Chart/toolbar/`:
  - `index.css` — собирает все подпакеты;
  - `layout/ChartToolbar.layout.css` — позиционирование контейнера, expanded-режим и якорь;
  - `controls/ChartToolbar.controls.css` — стили кнопок, состояний и тултипов;
  - `dropdown/ChartToolbar.dropdown.css` — выпадающие меню, календарь, скролл;
  - `groups/ChartToolbar.groups.css` — сегментированные группы и капсулы;
  - `tokens/ChartToolbar.tokens.css` — CSS-переменные для размеров и цветов;
  - `legacy/` — архив старого CSS для отката при необходимости.
### Результат
- Кнопка лупы и карточка тикера открывают один и тот же `SearchModal`, больше нет рекурсивного `socket.emit` и переполнения стека.
- Карточка имеет доступный тултип «Сменить тикер», который появляется по hover и `focus-visible`, не мешая клику.
- Структура стилей тулбара стала модульной: CSS разбит на подпакеты, удобные для дальнейшего сопровождения.
### Проверить
- По клику на лупу и на `TickerCard` открывается модалка поиска, ошибок в консоли нет.
- Тултип карточки отображается справа, стрелка указывает на карточку, на тач-устройствах подсказка не появляется.
- Папка `toolbar/` подключена в `ChartToolbar.js` (импорт `./toolbar/index.css`), стили применяются корректно.
## Изменения 2025-10-15
- Ослабили сетку графика: убрали вертикальные линии, горизонтальные сделали полупрозрачными, подписи шкалы цены приглушили (верх – `rgba(154,160,166,0.75)`, тёмный режим – `rgba(198,206,218,0.78)`).
- Нижняя шкала объёма тоже стала мягче — подписи `rgba(154,160,166,0.55)`.
- Свечной график теперь показывает аккуратную линию последней цены с лейблом; включена `lastPriceAnimation`.
- Линии MA/EMA стали толще (≈2.4px) и с прозрачностью (`rgba(52,152,219,.9)` и `rgba(243,156,18,.9)`), убрали лишние лейблы.
- Объём: гистограмма всегда стартует от нуля (`base: 0`), держит нижнюю панель ~22% высоты, цвета столбиков зависят от направления свечи.
- Добавлен задел под мультипанельный режим (charts/Pane, MultiPaneChart, series/candles|volume|rsi, store/useCandles). Пока эти файлы не активны по умолчанию, но легко включаются флагом в `ChartRenderer`.
### Основные изменения в файлах
- `src/components/Results/Chart/ChartCanvas.js` — вся косметика (сетка, last price, MA/EMA, объём, управление индикаторами).
- `src/components/Results/Chart/ChartRenderer.js` — оставлен в бою `ChartCanvas`, мультипанель отключена до финальной полировки.
- `src/components/Results/Chart/ChartToolbar.js`, `ChartContext.js` — индикаторы «Объём»/RSI взаимоисключающие (только один нижний).
- *Новые файлы* (архитектура мультипанели):
  - `src/charts/SyncBus.js`
  - `src/charts/ChartSyncController.js`
  - `src/charts/Pane.jsx`
  - `src/charts/MultiPaneChart.jsx`
  - `src/charts/series/candles.js`
  - `src/charts/series/volume.js`
  - `src/charts/series/rsi.js`
  - `src/store/useCandles.js`
### Что дальше
- Легенда индикаторов с цветными маркерами и сворачиванием настроек.
- RSI: опциональные уровни 30/70 (toggle).
- Перетаскиваемая высота панелей и сохранение layout, когда вернём мультипанель.
- Усилить метку последней цены (фон/цвет) при необходимости, добавить хоткеи для интервалов.
-В нём перечислены задачи следующего спринта: легенда индикаторов, управление уровнями RSI, возвращение и доработка мультипанельного режима (drag-resize, сохранение layout), усиление метки последней цены, хоткеи для интервалов, диффовые updateData, расширение воркера (Bollinger/MACD/VWAP), управление комнатами сокета, обновление README, тесты, Storybook и nice-to-have — SVG-экспорт, snapshots индикаторов, а также новый пункт про инструменты рисования (трендовые линии, уровни, фибы) и переключатель режима “рисование”.
