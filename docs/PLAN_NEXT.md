# План следующей итерации

## Фронтенд
- Вынести текущую Socket.IO логику (подписки initialData/updateData) в `src/hooks/useCandlesSocket.js`.
- Подключить хук в `App.js` / `ChartContainer`, сохранив текущий интерфейс `chartData`/`isChartLoading`.
- Убрать отладочные логи и временные флаги после переноса.

## Backend1 / Backend2
- Настроить `/api/dividends` backend1 → проксировать реальные данные с backend2 (параметр `searchQuery`).
- Обновить фронт (`REACT_APP_DIVIDENDS_URL` или переход через backend1), чтобы дивиденды брались из БД.
- Рассмотреть единый формат `/api/ticker` и `/api/news`, чтобы фронт можно было переключить на backend2 без доп. правок.

## Документация
- Зафиксировать в README фронтенда, где лежит `useCandlesSocket` и как его подключать.
- Обновить общий `README` / `RUNBOOK` (сервисы, порты, переменные окружения).

## Тесты и smoke-check
- Подготовить короткий чек-лист (REST + WS + фронт) для запуска после деплоя.
- Подумать о простых unit/интеграционных тестах для сервисов (db/cache) — хотя бы в виде TODO.
